import styled from "styled-components";

export const VideoPlayer = styled.header`
  .bgvideo {
    min-width: 100%;
    min-height: 100%;
    position: absolute;
    bottom: 0;
    right: 0;
    object-fit: contain;
    z-index: -1;
  }
`;
